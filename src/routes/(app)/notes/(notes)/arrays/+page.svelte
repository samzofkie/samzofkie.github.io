<h1>Arrays</h1>

<p>
  An <b>array</b> is a contiguous sequence of bytes in memory. Each element in an array can be accessed via an <b>index</b> in constant time.
</p>

<p>
  Array elements are typically of the same size, or if they are variable sized, they are usually represented via a pointer.
</p>
